name: deploy-staging (Deploy storybook environments to Github Pages)

on:
  push:
    branches:
      - release/*

concurrency:
  group: deploy-staging-${{ github.ref }}
  cancel-in-progress: true

jobs:
  react:
    if: github.repository == 'jeffchew/carbon-for-ibm-dotcom'
    runs-on: ubuntu-20.04
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js 14.x
        uses: actions/setup-node@v2
        with:
          node-version: '14.x'
          cache: 'yarn'
      - name: Install dependencies
        run: yarn install --offline
      - name: Build project
        run: yarn lerna run --stream --ignore @carbon/ibmdotcom-web-components --ignore @carbon/ibmdotcom-services-store build
      - name: Set env vars
        uses: ./.github/actions/set-dotenv
        with:
          env-file: packages/react/.env
        env:
          KALTURA_PARTNER_ID: ${{ secrets.KALTURA_PARTNER_ID }}
          KALTURA_UICONF_ID: ${{ secrets.KALTURA_UICONF_ID }}
          PROFILE_HOST: ${{ secrets.PROFILE_HOST }}
      - name: Building @carbon/ibmdotcom-react storybook
        run: yarn build-storybook
        working-directory: packages/react
      - name: Deploying @carbon/ibmdotcom-react (staging) storybook to IBM Cloud
        run: |
          rm -rf staging/react
          mkdir -p staging
          mv packages/react/storybook-static staging/react

          git config --global user.email ${{ secrets.BOT_EMAIL }}
          git config --global user.name ${{ secrets.BOT_NAME }}
          git fetch "https://git:${BOT_TOKEN}@github.com/jeffchew/carbon-for-ibm-dotcom.git" gh-pages

          git add staging/react
          git commit -m "chore(deploy): deploy React staging to GitHub Pages"

          if [ $? -eq 0 ]; then
            git push --force "https://git:${BOT_TOKEN}@github.com/jeffchew/carbon-for-ibm-dotcom.git" main:gh-pages > /dev/null 2>&1
          fi
#      - uses: act10ns/slack@v1
#        with:
#          status: ${{ job.status }}
#        if: failure()
  react-rtl:
    if: github.repository == 'jeffchew/carbon-for-ibm-dotcom'
    runs-on: ubuntu-20.04
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js 14.x
        uses: actions/setup-node@v2
        with:
          node-version: '14.x'
          cache: 'yarn'
      - name: Install dependencies
        run: yarn install --offline
      - name: Build project
        run: yarn lerna run --stream --ignore @carbon/ibmdotcom-web-components --ignore @carbon/ibmdotcom-services-store build
      - name: Set env vars
        uses: ./.github/actions/set-dotenv
        with:
          env-file: packages/react/.env
        env:
          KALTURA_PARTNER_ID: ${{ secrets.KALTURA_PARTNER_ID }}
          KALTURA_UICONF_ID: ${{ secrets.KALTURA_UICONF_ID }}
          PROFILE_HOST: ${{ secrets.PROFILE_HOST }}
          REACT_STORYBOOK_USE_RTL: true
      - name: Building @carbon/ibmdotcom-react (RTL) storybook
        run: yarn build-storybook
        working-directory: packages/react
      - name: Deploying @carbon/ibmdotcom-react (RTL) storybook to IBM Cloud
        run: |
          rm -rf staging/react-rtl
          mkdir -p staging
          mv packages/react/storybook-static staging/react-rtl

          git config --global user.email ${{ secrets.BOT_EMAIL }}
          git config --global user.name ${{ secrets.BOT_NAME }}
          git fetch "https://git:${BOT_TOKEN}@github.com/jeffchew/carbon-for-ibm-dotcom.git" gh-pages

          git add staging/react-rtl
          git commit -m "chore(deploy): deploy React (RTL) staging to GitHub Pages"

          if [ $? -eq 0 ]; then
            git push --force "https://git:${BOT_TOKEN}@github.com/jeffchew/carbon-for-ibm-dotcom.git" main:gh-pages > /dev/null 2>&1
          fi
#      - uses: act10ns/slack@v1
#        with:
#          status: ${{ job.status }}
#        if: failure()
  web-components:
    if: github.repository == 'jeffchew/carbon-for-ibm-dotcom'
    runs-on: ubuntu-20.04
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js 14.x
        uses: actions/setup-node@v2
        with:
          node-version: '14.x'
          cache: 'yarn'
      - name: Install dependencies
        run: yarn install --offline
      - name: Build project
        run: yarn lerna run --stream --ignore @carbon/ibmdotcom-react build
      - name: Set env vars
        uses: ./.github/actions/set-dotenv
        with:
          env-file: packages/web-components/.env
        env:
          KALTURA_PARTNER_ID: ${{ secrets.KALTURA_PARTNER_ID }}
          KALTURA_UICONF_ID: ${{ secrets.KALTURA_UICONF_ID }}
          PROFILE_HOST: ${{ secrets.PROFILE_HOST }}
      - name: Building @carbon/ibmdotcom-web-components (staging) storybook
        run: yarn build-storybook
        working-directory: packages/web-components
      - name: Deploying @carbon/ibmdotcom-web-components (staging) storybook to IBM Cloud
        run: |
          rm -rf staging/web-components
          mkdir -p staging
          mv packages/web-components/storybook-static staging/web-components

          git config --global user.email ${{ secrets.BOT_EMAIL }}
          git config --global user.name ${{ secrets.BOT_NAME }}
          git fetch "https://git:${BOT_TOKEN}@github.com/jeffchew/carbon-for-ibm-dotcom.git" gh-pages

          git add staging/web-components
          git commit -m "chore(deploy): deploy Web Components staging to GitHub Pages"

          if [ $? -eq 0 ]; then
            git push --force "https://git:${BOT_TOKEN}@github.com/jeffchew/carbon-for-ibm-dotcom.git" main:gh-pages > /dev/null 2>&1
          fi
#      - uses: act10ns/slack@v1
#        with:
#          status: ${{ job.status }}
#        if: failure()
  web-components-rtl:
    if: github.repository == 'jeffchew/carbon-for-ibm-dotcom'
    runs-on: ubuntu-20.04
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js 14.x
        uses: actions/setup-node@v2
        with:
          node-version: '14.x'
          cache: 'yarn'
      - name: Install dependencies
        run: yarn install --offline
      - name: Build project
        run: yarn lerna run --stream --ignore @carbon/ibmdotcom-react build
      - name: Set env vars
        uses: ./.github/actions/set-dotenv
        with:
          env-file: packages/web-components/.env
        env:
          KALTURA_PARTNER_ID: ${{ secrets.KALTURA_PARTNER_ID }}
          KALTURA_UICONF_ID: ${{ secrets.KALTURA_UICONF_ID }}
          PROFILE_HOST: ${{ secrets.PROFILE_HOST }}
          STORYBOOK_IBMDOTCOM_WEB_COMPONENTS_USE_RTL: true
      - name: Building @carbon/ibmdotcom-web-components RTL storybook
        run: yarn build-storybook
        working-directory: packages/web-components
      - name: Deploying @carbon/ibmdotcom-web-components RTL storybook to IBM Cloud
        run: |
          rm -rf staging/web-components-rtl
          mkdir -p staging
          mv packages/web-components/storybook-static staging/web-components-rtl

          git config --global user.email ${{ secrets.BOT_EMAIL }}
          git config --global user.name ${{ secrets.BOT_NAME }}
          git fetch "https://git:${BOT_TOKEN}@github.com/jeffchew/carbon-for-ibm-dotcom.git" gh-pages

          git add staging/web-components-rtl
          git commit -m "chore(deploy): deploy Web Components (RTL) staging to GitHub Pages"

          if [ $? -eq 0 ]; then
            git push --force "https://git:${BOT_TOKEN}@github.com/jeffchew/carbon-for-ibm-dotcom.git" main:gh-pages > /dev/null 2>&1
          fi
#      - uses: act10ns/slack@v1
#        with:
#          status: ${{ job.status }}
#        if: failure()
